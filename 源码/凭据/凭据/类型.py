"""凭证引用能力缝的客户端安全类型面：引用品牌以及该缝的 Cordis 事件声明。只有类型——没有运行时代码，这里也不会碰到仅 Host 的符号，因此 Client 编译面读到的签名与 Host 发出的完全一致。"""

def 凭证引用品牌(原始):#把字符串打成凭证引用品牌，不做校验
    """把字符串打成凭证引用品牌。一条凭证的名义引用是 POSIX 风格的环境变量名。不做校验。"""
    return 原始#原样返回并打成凭证引用，不做校验

# 事件 credentials/updated(ref) @mode emit：提供方管理的凭证源已提交变更——一次 set、一次 unset，或观察到的外部存储编辑。进程环境的环境变量变化不可观察，也从不发出。监听失败（同步抛出与异步拒绝一样）会被吞掉并记日志，不改变已提交操作的结果；唯有带 INVARIANT 码的失败会在每个监听器都跑完后重新抛出；该重抛只从同步监听器到达发出方，因此本事件上的不变量检查不得写成 async 函数。参数 ref 为存储值已变更的引用。
