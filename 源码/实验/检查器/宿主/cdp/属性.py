"""Host 属性枚举从不经 Host source 桥。

对齐上游 `host/cdp/properties.ts`。公开面仅中文名。
"""
from .对象 import 拒绝对象桥操作#拒绝对象操作

__all__=['拒绝属性桥操作']#仅中文公开名

def 拒绝属性桥操作():#拒绝属性桥操作
    """拒绝必须使用 Worker 拥有的原生 inspector session 的属性请求。"""
    return 拒绝对象桥操作('client-runtime/get-properties')#委托对象拒绝
