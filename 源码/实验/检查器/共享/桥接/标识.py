"""跨界域检查器桥接拥有的不透明标识。

对齐上游 `shared/bridge/ids.ts`。公开面仅中文名。
"""
from ..身份 import 检查器标识,检查器id#品牌化

__all__=[#仅中文公开名
    '检查器标识','检查器id',
    '检查器源标识','检查器源世代',
    '客户端运行时会话标识','客户端运行时请求标识',
    '客户端源会话标识','客户端源请求标识','客户端远程对象句柄',
]#公开面结束

def 检查器源标识(值):#源标识
    """一个逻辑观测源的稳定身份。"""
    return 检查器标识(值)#烙印

def 检查器源世代(值):#源世代
    """一个源连接世代的身份。"""
    return 检查器标识(值)#烙印

def 客户端运行时会话标识(值):#Runtime会话标识
    """一个 DevTools 连接的 Client Runtime 状态身份。"""
    return 检查器标识(值)#烙印

def 客户端运行时请求标识(值):#Runtime请求标识
    """一次进行中的 Worker→Client Runtime 操作身份。"""
    return 检查器标识(值)#烙印

def 客户端源会话标识(值):#源目录会话标识
    """一个 DevTools 连接的 Client 源目录会话身份。"""
    return 检查器标识(值)#烙印

def 客户端源请求标识(值):#源请求标识
    """一次进行中的 Worker→Client 源操作身份。"""
    return 检查器标识(值)#烙印

def 客户端远程对象句柄(值):#远程对象句柄
    """一个 Client Runtime 会话内所保留对象的不透明引用。"""
    return 检查器标识(值)#烙印
