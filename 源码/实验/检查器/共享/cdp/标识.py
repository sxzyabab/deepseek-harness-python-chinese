"""规范化界域后端拥有的不透明标识。

对齐上游 `shared/cdp/ids.ts`。公开面仅中文名。
"""
from ..身份 import 检查器标识#品牌化

__all__=['检查器领域标识','运行时后端对象句柄','运行时脚本键']#仅中文公开名

def 检查器领域标识(值):#界域标识
    """一个活动 Host 或 Client 界域化身的 Worker 身份。"""
    return 检查器标识(值)#烙印

def 运行时后端对象句柄(值):#后端对象句柄
    """仅由其界域会话解释的、后端拥有的对象句柄。"""
    return 检查器标识(值)#烙印

def 运行时脚本键(值):#脚本键
    """界域目录中一个脚本的、与后端无关的身份。"""
    return 检查器标识(值)#烙印
