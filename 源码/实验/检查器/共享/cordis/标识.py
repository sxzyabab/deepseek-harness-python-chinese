"""界域本地 Cordis 对象注册表拥有的不透明标识。

对齐上游 `shared/cordis/ids.ts`。公开面仅中文名。
"""
from ..身份 import 检查器标识#品牌化

__all__=['检查器对象注册表标识','检查器对象句柄']#仅中文公开名

def 检查器对象注册表标识(值):#对象注册表标识
    """在快照中点名对象、并由界域本地表保留的那张表的身份。"""
    return 检查器标识(值)#烙印

def 检查器对象句柄(值):#对象句柄
    """界域本地注册表所保留对象的不透明引用。"""
    return 检查器标识(值)#烙印
