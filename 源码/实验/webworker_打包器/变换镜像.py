"""打包镜像体所对照的包装契约，以及打包遍次消费的镜像条目类型。

两侧共用一次变换——打包遍用运行时自己的 `降低模块源码` 降级，从不重实现——
镜像记录其降级所对照的契约版本。对照不同包装契约发出的体在挂载时被拒绝，而不是运行时半工作。

对齐上游 `webworker-packer/src/transform-image.ts`。公开面仅中文名。
"""
from ..webworker_运行时 import 降低版本#降低契约版本

__all__=[#仅中文公开名
    '包装契约',
]#公开面结束

#镜像文件表：键为相对虚拟根的路径，值为字节（对齐ImageFiles）
包装契约=降低版本#打包体所对照的包装契约
