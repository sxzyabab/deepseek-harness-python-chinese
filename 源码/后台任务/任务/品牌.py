"""dsh-jobs 拥有的品牌 id，贯穿注册表、面向模型的控制面和客户端线路。

它单独成叶，因为包根和类型面都经所有者与监听器签名到达 dsh-agent，客户端程序即使作为类型也无法解析。浏览器安全的消费方在此导入该 id；带品牌本身来自零依赖的 dsh-brand。
"""

def 任务标识(标识):#把字符串品牌化为任务标识
    """把字符串品牌化为任务标识。注册表生成 `<kind>-N`；可预测 id 靠所有者授权而不是保密。不做校验。"""
    return 标识#运行时值不变，仅做类型品牌
