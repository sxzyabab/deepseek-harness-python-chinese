# tool_cordis 目录迁移进度

## 状态

SERVICE：服务目录_01..18.上游.ts 本包 55 键；原版 api-catalog SERVICE_API 现为 71 键。
本包不足时期望条数 71，按键去重从追踪 api-catalog.ts 补入（含 workspaceFiles.readAll/readRelated 等）。
本包独有 `apiProxy` 仍保留。EVENT：事件目录_01..02.上游.ts 全量 56/56 本包，缺口 0。
TYPE：类型目录_01..05.txt → 类型目录表，去重名对齐上游，缺口 0。
INHERITED_CTX：接口目录.py，缺口 0。

## 装载

接口目录 ← 服务目录表 / 事件目录表 / 类型目录表
上游目录.py：单文件优先；否则有序分片；本包条数不足期望时按键去重补原版 api-catalog.ts。

## SERVICE 分片（本包已落 55；另 17 键运行时从原版补）

01 agentDefaultModel, agentLoop（2）
02 agentPresets（1）
03 agents（1）
04 apiProxy .. directoryPicker（9；apiProxy 本包独有）
05 e2b, fs（2）
06 goals, invariants（2）
07 空（已并入 06/08，避重键）
08 jobs, llm（2）
09 lsp .. sandboxPolicy（6）
10 sessionPersistence, sessionProjectionCache（2）
11 sessionProjections, sessionReferenceResolver（2）
12 sessionQuery（1）
13 sessions, sessionTelemetry, sessionTitle（3）
14 settings .. storageDomain（7）
15 subagents（1）
16 subprocess, systemPrompt, timer, tokenMeter, toolResultPruner（5；terminals 在 17）
17 terminals, tools（2）
18 typert .. workspaceRegistry（7；缺 webhookRuntime / workspaceController / workspaceFiles，由原版补）
合计本包 55；原版另补 17 → 运行时约 72（含 apiProxy）

## EVENT 分片（本包已落）

01 agent-loop/config-start-failed .. fs/write-intent（27）
02 goal/activation-changed .. workflow/start（29）合计 56

## 勿用

类型_甲..丁、服务_甲/乙/丙（切片演示）不参与主装载。
