# tool_cordis 目录迁移进度

## 状态

SERVICE：服务目录_01..18.上游.ts 本包全量 55/55，缺口 0（原版 api-catalog SERVICE_API 实为 55 键；旧记 53 已纠）。
EVENT：事件目录_01..02.上游.ts 全量 55/55 本包，缺口 0。
TYPE：类型目录_01..05.txt → 类型目录表，去重名对齐上游 509，缺口 0。
INHERITED_CTX：接口目录.py，缺口 0。

## 装载

接口目录 ← 服务目录表 / 事件目录表 / 类型目录表
目录数据.py：兼容再导出三表。
上游目录.py：单文件 `服务目录.上游.ts` / `事件目录.上游.ts` 优先；否则有序分片 `服务目录_*.上游.ts` / `事件目录_*.上游.ts`；本包条数不足期望时按键去重补原版 api-catalog.ts。

上游 api-catalog.ts 约 4749 行（SERVICE~2070 + EVENT~450 + TYPE~2040 + 头尾）。

## TYPE 片段（本包内嵌）

01 AdapterRegistrationHandle .. DynamicCordisRunRequest
02 EditGoalRequest .. ManualCompactAgentContext
03 Message .. SessionEventType
04 SessionEventWindow .. ToolDispatchExecution
05 ToolErrorInfo .. WorkflowStopReason

## SERVICE 分片（本包已落 55）

01 agentDefaultModel, agentLoop（2）
02 agentPresets（1）
03 agents（1）
04 apiProxy .. directoryPicker（9）
05 e2b, fs（2）
06 goals, invariants（2）
07 空（已并入 06/08，避重键）
08 jobs, llm（2）
09 lsp .. sandboxPolicy（6）
10 sessionPersistence, sessionProjectionCache（2）
11 sessionProjections, sessionReferenceResolver（2）
12 sessionQuery（1）
13 sessions, sessionTelemetry, sessionTitle（3）
14 settings .. storageDomain（7）
15 subagents（1）
16 subprocess, systemPrompt, timer, tokenMeter, toolResultPruner（5；terminals 在 17）
17 terminals, tools（2）
18 typert .. workspaceRegistry（7）
合计 55

## EVENT 分片（本包已落）

01 agent-loop/config-start-failed .. fs/write-intent（27）
02 goal/changed .. workflow/start（28）合计 55

## 缺口（相对 4749 行原文落盘）

TYPE 紧凑行 509 条已落盘。
EVENT 上游 TS 约 450 行已落盘（两片）。
SERVICE 上游 TS 已落盘（十八片，55 键），缺口 0。

## 勿用

类型_甲..丁、服务_甲/乙/丙（切片演示）不参与主装载。
