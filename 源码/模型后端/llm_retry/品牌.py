"""一次请求步重试链中每次尝试共用的稳定身份。

对齐上游 `llm-retry/src/brand.ts`。公开面仅中文名；无英文别名。
"""

__all__=('重试身份',)#仅中文公开名

def 重试身份(标识):#把实现签发的重试链身份品牌化
    """把实现签发的重试链身份品牌化。

    @param 标识: 不透明的重试身份字符串
    @returns 同一字符串，已品牌化；不做校验
    """
    return 标识#仅做类型品牌，不校验
