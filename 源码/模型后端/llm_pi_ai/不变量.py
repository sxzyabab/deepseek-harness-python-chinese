"""`@deepseek-ai/dsh-llm-pi-ai` 的本包拥有不变量配套。

对齐上游 `llm-pi-ai/src/invariant.ts`。公开面仅中文名；无英文别名。

无运行时不变量：适配器注册与配置校验拥有可变值关系；本包不暴露独立事件序列。
"""
from ...依赖 import cordis#外部依赖胶水
包名='@deepseek-ai/dsh-llm-pi-ai'#本包的不变量所有权名
名称='llm-pi-ai-invariant'#配套不变量插件名（字面量不译）
注入=['invariants']#预留包所有权前必须具备的服务

__all__=('包名','名称','注入','安装','应用')#仅中文公开名

def 安装(*位置参数):#空安装器，不挂运行时检查
    """无运行时不变量：适配器注册与配置校验拥有可变值关系。位置参数由登记约定传入，此处忽略。"""
    return#没有可交叉核对的事件序列，登记约定传入的位置参数忽略

def 应用(上下文对象):#应用不变量配套插件
    """注册本包的不变量配套，返回安装成功后已登记贡献的拆除器。"""
    return 已兑现(上下文对象.invariants.注册(包名,安装))#登记空贡献；拆除器立刻兑现
