"""dsh-llm 拥有的品牌 id：工具调用关联与提供方请求诊断。

对齐上游 `llm/src/brand.ts`。公开面仅中文名；无英文别名。
"""

__all__=('消息标识','调用标识','提供方请求标识','推理力度标识')#仅中文公开名

def 消息标识(标识):#品牌化消息标识
    """把字符串品牌化为消息标识，不做校验。"""
    return 标识#仅做类型品牌，不校验

def 调用标识(标识):#品牌化工具调用关联标识
    """把字符串品牌化为工具调用关联标识，不做校验。"""
    return 标识#仅做类型品牌，不校验

def 提供方请求标识(标识):#品牌化提供方请求标识
    """把提供方签发的请求标识品牌化，不做校验。"""
    return 标识#仅做类型品牌，不校验

def 推理力度标识(标识):#品牌化推理力度标识
    """把适配器拥有的推理力度标识品牌化，不做校验。"""
    return 标识#仅做类型品牌，不校验
