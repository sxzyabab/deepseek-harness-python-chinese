"""dsh-lsp 拥有的品牌 id：语言服务器提供方标识，提供方在 `ctx.lsp` 上预留的不透明身份。

对齐上游 `lsp/src/brand.ts`。公开面仅中文名。`Branded` 原语位于 dsh-brand；把类型与工厂放在一起，让包根用同一名字再导出两者。
"""

__all__=['语言服务器提供方标识']#仅中文公开名

def 语言服务器提供方标识(标识):#把字符串打成提供方身份品牌
    """把字符串打成语言服务器提供方标识品牌。不做校验——注册表在注册时拒绝空 id。"""
    return 标识#运行时值不变，仅做类型品牌
