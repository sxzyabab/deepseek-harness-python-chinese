"""无 React 的快照存储引擎与声明式壳。

对齐上游 `@deepseek-ai/dsh-client-store`。公开面仅中文名。
引擎产物为裸可观察源；钩子在 ui_渲染器 侧合成。
"""
from ..运行时.约定.存储 import 浅相等,创建快照存储,声明存储#引擎实现

__all__=[#仅中文公开名
    '浅相等',
    '创建快照存储',
    '声明存储',
    '通知订阅者们',
    '应用',
]#公开面结束

def 通知订阅者们(监听者们,标签,*参数):#通知订阅者
    """逐个通知，单个回调失败不饿死其余。"""
    for 监听 in list(监听者们):#复制后派发
        try:#单回调
            监听(*参数)#调用
        except Exception as 错误:#失败
            print(标签+' subscriber failed:',错误)#打出

def 应用():#宿主插件体
    """本包导出库引擎，无宿主侧行为。"""
    return#空 apply
