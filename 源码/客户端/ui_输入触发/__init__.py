"""斜杠触发插件，节点半边。

对齐上游 `@deepseek-ai/dsh-client-ui-input-trigger`。公开面仅中文名。纯界面插件：空 apply 仅占 Loader 行。
"""

__all__=['应用']#仅中文公开名

def 应用():#宿主插件体
    """此斜杠触发插件无宿主侧行为。"""
    return#空 apply，仅占 Loader 行
