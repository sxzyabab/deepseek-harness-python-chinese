"""浏览器会话 UI 适配器的宿主 loader 入口。

对齐上游 `@deepseek-ai/dsh-client-ui-session`。公开面仅中文名。
不提供任何宿主侧行为；对象层在客户端/运行时。
"""
__all__=['应用']#仅中文公开名

def 应用():#宿主插件体
    """空 apply，浏览器入口在客户端/。"""
    return#空 apply
