from ..运行时.约定.存储 import 浅相等,创建快照存储,声明存储#引擎实现

__all__=[#仅中文公开名
    '浅相等',
    '创建快照存储',
    '声明存储',
    '通知订阅者',
    '应用',
]#公开面结束

def 通知订阅者(监听者列表,标签,*参数):#通知订阅者
    """逐个通知，单个回调失败不饿死其余。"""
    for 监听 in list(监听者列表):#复制后派发
        try:#单回调
            监听(*参数)#调用
        except Exception as 错误:#订阅者回调契约未定
            print(标签+' 订阅者失败:',错误)#打出

def 应用():
    """本包导出库引擎，无宿主侧行为。"""
    return#空 apply

apply=应用#框架槽
