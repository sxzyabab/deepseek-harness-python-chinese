"""Web 提问插件的节点半边。

故意留空。把 `ask_user_question` 挂在这里会进入工具注册表的 GLOBAL 层，
于是无论哪个预设组合进来，每个智能体都能看到它——两工具基准预设实际露出三个，
本地撰写的 `bash-only` 预设则露出两个。渲染提问是宿主 UI 能力；拥有该工具是智能体能力，
只有预设才能决定。`tool-ask-user` 行应放在想要它的预设里（以及没有预设的 TUI 组合里）。

对齐上游 `@deepseek-ai/dsh-client-ui-user-questions`。公开面仅中文名。
"""

__all__=['应用']#仅中文公开名

def 应用():#空 apply，仅占 Loader 行
    """宿主插件体——面向模型的工具按预设组装，不在此处。"""
    return#无宿主侧行为
