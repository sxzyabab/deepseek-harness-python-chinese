"""向导签身份、页面地址方案与播种工厂。

对齐上游 `ui-sidebar-right/src/client/contract/seed.ts`。公开面仅中文名。
存储为每新窗格播种向导签；向导域以同一种类登记类型；套件视 kind 为不透明。
"""

__all__=['向导种类','页面地址','造向导标签']#仅中文公开名

向导种类='guide'#向导签种类（线路字面量）


def 页面地址(种类):
    """页面签记录地址：`sidebar://<kind>`。调用方只点名种类，不拼地址。"""
    return 'sidebar://'+种类#页面地址


def 造向导标签(标识,标题):
    """新窗格播种用的向导签记录。标题在铸造时捕获，进操作序列后语义不变。"""
    return {#标签记录（跨包 dict）
        'id':标识,#签标识
        'kind':向导种类,#种类
        'contentId':页面地址(向导种类),#内容身份
        'title':标题,#芯片标题
    }#记录结束
