"""文件/会话引用插件，节点半边。

对齐上游 `@deepseek-ai/dsh-client-ui-reference`。公开面仅中文名。
纯 UI 插件：空 apply 只为让插件出现在 host cordis.yml / Loader 中。
"""
__all__=['应用']#仅中文公开名

def 应用():#宿主插件体
    """纯 UI 插件：浏览器半边经客户端/发现。"""
    return#空 apply
