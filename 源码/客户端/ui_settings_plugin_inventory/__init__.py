"""从 `./client` 导出的浏览器实现的宿主加载器入口。

对齐上游 `@deepseek-ai/dsh-client-ui-settings-plugin-inventory`。公开面仅中文名。
"""

__all__=['应用']#仅中文公开名

def 应用():#宿主插件体
    """插件库存页无宿主侧行为。"""
    return#空 apply，仅占 Loader 行
