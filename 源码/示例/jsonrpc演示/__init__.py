"""仅二进制的应用包：通用入口与打包入口发现外部 cordis.yml 并拥有进程退出。

对齐上游 `@deepseek-ai/dsh-sdk-jsonrpc-demo`（`示例/jsonrpc-demo/src/index.ts`）。
本模块不导出组合插件；配置自行选择是否加载 sdk_jsonrpc_server 服务插件。
"""

__all__=[]#空模块，仅作包入口
