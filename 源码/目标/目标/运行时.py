"""目标域的运行时构造函数与协议常量。"""
from ..llm import 装备错误#导入 harness 错误基类

目标变更版本=1#嵌入轮次零消息来源中的目标变更版本

def 目标标识(标识):#把字符串打成目标 id 品牌
    """把字符串打成目标 id 品牌。原始目标标识符原样返回，仅做编译期品牌。"""
    return 标识#运行时值不变

class 目标错误(装备错误):#目标域边界返回的错误
    """目标域边界返回的错误，携带稳定可按机器路由的分类码。"""
    def __init__(自身,消息,码):#按消息与稳定错误码构造
        """记下人类可读拒绝原因与稳定分类码。"""
        装备错误.__init__(自身,消息,码)#交给装备错误保存消息与码
        自身.name='GoalError'#本域错误名
