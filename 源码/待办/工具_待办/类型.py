"""待办域的纯类型：`todos` 投影键声明及其载荷类型的唯一归属，不含本包宿主侧值导入。

对齐上游 `tool-todo/src/types.ts`。公开面仅中文名。两处命名空间投影为它服务——`./类型` 给宿主消费方，`./客户端` 给客户端聚合——内容零重复。

投影键 `todos`：智能体当前的整表待办（最近一次 `todo/write` 快照），首次写入前为 `None`。整值规则：每次 `todo/write` 都携带完整替换列表，因此折叠是后写覆盖。
"""
from session.类型 import 待办条目,待办状态#会话拥有的待办词表

__all__=['待办条目','待办状态']#仅中文公开名
