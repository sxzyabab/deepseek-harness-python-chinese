"""待办域的客户端命名空间投影：对本包类型出口的纯再导出。

对齐上游 `tool-todo/src/client.ts`。公开面仅中文名。客户端代码只导入客户端命名空间，因此本模块把 `./类型` 提供给宿主消费方的同一份内容投影过去——零重复。
"""
from .类型 import 待办条目,待办状态#与宿主类型面同一出处

__all__=['待办条目','待办状态']#仅中文公开名
