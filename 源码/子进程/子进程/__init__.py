"""子进程能力缝（ctx.subprocess）的服务定义。

对齐上游 `subprocess/src/index.ts`。公开面仅中文名；无英文别名。
执行世界里的可执行文件查找、带原始或收集 stdio 的完全指定受管进程树，以及一个终端进程原语。
命令默认值、shell 语义、截止、协议分帧、终端就绪和展示属于消费方。本地实现在 subprocess_local。
"""
import os,re#父环境与敏感名模式
from cordis import 服务#从 Cordis 导入服务基类
from .类型 import (
    托管环境前缀,#托管环境变量前缀
    托管环境键,#托管键品牌
    托管环境,#DSH 环境表
    已收集输出字段,#已收集输出键
    已收集输出,#已收集输出
    收集模式字段,#收集模式键
    溢出配置字段,#溢出配置键
    收集模式,#收集模式
    溢出配置,#溢出配置
    三路输入输出字段,#三路 stdio 键
    三路输入输出,#三路 stdio
    启动规格字段,#启动规格键
    启动规格,#启动规格
    退出事实字段,#退出事实键
    退出事实,#退出事实
    一次读取字段,#一次读取键
    一次读取,#一次读取
    输出读取器字段,#输出读取器键
    输出读取器,#输出读取器
    收集输出集合字段,#收集输出集合键
    收集输出集合,#收集输出集合
    句柄字段,#子进程句柄键
    句柄,#子进程句柄
    终端信号,#终端信号
    终端启动规格字段,#终端启动规格键
    终端启动规格,#终端启动规格
    前台组字段,#前台进程组键
    前台组,#前台进程组
    终端句柄字段,#终端句柄键
    终端句柄,#终端句柄
    标准输入模式,#标准输入模式
    标准输出模式,#标准输出模式
)#再导出子进程公开类型

__all__=(#仅中文公开名；无英文别名
    '敏感环境模式','擦洗父环境','子进程运行时','默认',
    '托管环境前缀','托管环境键','托管环境',
    '已收集输出字段','已收集输出',
    '收集模式字段','溢出配置字段','收集模式','溢出配置',
    '三路输入输出字段','三路输入输出',
    '启动规格字段','启动规格',
    '退出事实字段','退出事实',
    '一次读取字段','一次读取',
    '输出读取器字段','输出读取器',
    '收集输出集合字段','收集输出集合',
    '句柄字段','句柄',
    '终端信号','终端启动规格字段','终端启动规格',
    '前台组字段','前台组',
    '终端句柄字段','终端句柄',
    '标准输入模式','标准输出模式',
)#公开面结束

敏感环境模式=re.compile(r'KEY|PASSWORD|SECRET|TOKEN',re.I)#凭证形态环境名模式；仓库内每个启动器共用

def 擦洗父环境():#去掉凭证形态名字和全部 DSH_* 名字后的环境父环境
    """去掉凭证形态名字和全部 DSH_* 名字后的环境父环境——每个 harness 子进程起步的规范基座。

    PATH、HOME、locale 和代理变量会留下；harness 身份从不隐式泄漏。
    有意转发的凭证或当前 DSH_* 事实走规格的显式 env，在这次擦洗之后合并。
    两次擦洗都大小写不敏感。导出为普通函数，让无法走服务的启动器共用同一份擦洗定义。
    """
    环境={}#新环境对象
    for 键,值 in os.environ.items():#遍历父环境
        if 值 is not None and 敏感环境模式.search(键) is None and not 键.upper().startswith(托管环境前缀):#非凭证且非 DSH_ 才保留
            环境[键]=值#写入擦洗后的环境
    return 环境#返回擦洗后的环境

class 子进程运行时(服务):#子进程运行时服务定义
    """抽象子进程服务。子类实现启动，并把子类当插件加载——它登记为 ctx.subprocess。

    每个上下文一个实现；再加载第二个会抛错。语义：
    - 可执行路径属于与已挂载文件系统提供方共享的同一个执行世界。
    - 启动立即返回存活句柄；done 在进程 close 时兑现退出事实，仅 spawn 级失败会拒绝。
    - 收集模式读取器基于偏移且不消费；有损失的读取报告截断与溢出文件。
    - 终止按 SIGTERM→宽限→SIGKILL 升级且树范围；等待退出观察整树存活。
    - 销毁服务会终止所有仍在跑的受管进程并等待它们退出。
    - 启动终端把终端分配、文本传输、前台组、发信号和整会话静止收在一个可等待的终止方法后面。
    公开方法仅中文：解析可执行文件、启动、启动终端。
    """
    def __init__(自身,上下文对象):#用 Cordis 上下文构造服务
        """用 Cordis 上下文构造服务。"""
        super().__init__(上下文对象,'subprocess')#向宿主登记名为 subprocess 的服务

    def 解析可执行文件(自身,命令,环境=None,信号=None):#解析可执行文件路径
        """在本提供方的执行世界里解析一个已配置的可执行文件。

        绝对路径会核验；裸名使用提供方擦洗过的 PATH 加上显式环境覆盖。带分隔符的相对路径会被拒绝。
        """
        raise NotImplementedError('子进程运行时.解析可执行文件')#子类必须实现

    def 启动(自身,规格):#启动受管子进程
        """从完全指定的规格启动一个受管子进程；本缝不套默认值。返回存活进程句柄。"""
        raise NotImplementedError('子进程运行时.启动')#子类必须实现

    def 启动终端(自身,规格):#启动终端会话
        """分配真实终端并启动一个所拥有的进程会话。这是唯一的非管道进程原语。返回分配成功后的存活终端句柄。"""
        raise NotImplementedError('子进程运行时.启动终端')#子类必须实现

默认=子进程运行时#默认导出该服务定义类（中文名；无英文 default 别名）
