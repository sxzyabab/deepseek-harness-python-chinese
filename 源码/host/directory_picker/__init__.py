"""`ctx.directoryPicker` 能力缝的服务定义：网页图形宿主如何让操作者选择工作区目录。

对齐上游 `@deepseek-ai/dsh-host-directory-picker`。公开面仅中文名。各后端的差异在交互形态，不只是实现机制，因此服务暴露带判别标签的能力，而不是一套方法：`native` 后端在宿主显示器上打开一次操作系统选择器，`browse` 后端则为应用内浏览器提供列举/创建原语。消费方按 `capability().kind` 分支；对未知 kind 的文档化默认是隐藏选择入口而不是失败。本包默认导出服务类。
"""
from ...依赖 import cordis#外部依赖胶水
服务=cordis.服务#Cordis 服务基类
from .类型 import (
    目录选择原生能力,#原生能力
    目录条目,#目录条目
    目录列举,#列举结果
    目录选择浏览能力,#浏览能力
    目录选择能力表,#可合并能力表
    目录选择能力,#能力联合
    目录选择错误码,#业务失败码
    目录选择错误,#带业务码失败
)#类型面

__all__=[#仅中文公开名
    '目录选择器',
    '目录选择原生能力',
    '目录条目',
    '目录列举',
    '目录选择浏览能力',
    '目录选择能力表',
    '目录选择能力',
    '目录选择错误码',
    '目录选择错误',
]#公开面结束

class 目录选择器(服务):#抽象目录选择服务
    """抽象的目录选择服务。子类实现 `capability()` 并作为插件加载——注册为 `ctx.directoryPicker`。能力对象在服务生命周期内必须稳定。"""
    def __init__(自身,上下文):#按上下文注册 directoryPicker 服务
        """以 directoryPicker 为服务名挂到上下文。"""
        super().__init__(上下文,'directoryPicker')#登记服务槽

    def capability(自身):#此后端的交互能力
        """返回消费方按判别标签分支的能力对象。"""
        raise NotImplementedError('目录选择器.capability')#子类实现
