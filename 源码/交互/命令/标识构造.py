"""dsh-commands 自有的标识构造：把命令生命周期配对贯穿会话日志、线路准入响应和客户端流程配对。

`标识构造` 原语住在标识构造包；本模块是纯类型/构造出口（无 cordis 导入，无模块扩增），让线路与客户端程序能点名该标识而不加载宿主插件的 Context 合并——与 llm/标识构造 同一形态。
"""

def 命令标识(标识):#把字符串打成命令生命周期配对标识
    """把字符串打成命令标识。一次命令执行的 command/run 与 command/done 生命周期记录彼此配对，并与 command.execute 准入响应配对。由执行器铸造，按服务实例单调。原始不透明配对 id 原样返回，仅做编译期标识；不做校验。"""
    return 标识#运行时值不变，仅做标识构造
