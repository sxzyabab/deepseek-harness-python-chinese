"""JSON 后端的原子整文件替换。"""
import os,secrets#路径与随机临时名
from ...工具.原子写入 import 原子写文件#原子写
__all__=['原子写']#仅中文公开名

def 原子写(路径,数据):#耐久替换整文件
    """用完整新内容耐久替换目标文件。"""
    原子写文件(路径,数据,{'mode':0o600,'dirMode':0o700})#独占临时再改名；原子写文件按 UTF-8 编码
