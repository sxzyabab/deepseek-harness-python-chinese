"""单语句工作线程入口：在真实父端口上启动驱动工作线程会话。逻辑留在会话模块里，以便同进程队列覆盖；在主线程导入本入口会走到要求父端口的失败路径。"""
from .会话 import 要求父端口,驱动工作线程会话#导入端口校验与会话驱动

# 本入口供独立工作线程加载；宿主侧直接调用驱动工作线程会话，不经本入口。
# 若在主线程无父端口环境下导入并执行，要求父端口会失败。
def 主(父端口=None,工作线程数据=None):#在父端口上启动会话
    """在真实父端口上启动会话。父端口与工作线程数据由拉起方注入。"""
    驱动工作线程会话(要求父端口(父端口),工作线程数据)#在真实父端口上启动会话
