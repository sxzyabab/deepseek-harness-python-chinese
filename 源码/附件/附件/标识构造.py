"""附件标识构造。对齐上游 attachment/src/brand.ts。"""
from ...工具.标识构造 import 标识构造#名义类型原语
__all__=['附件标识','图像变体标识']#仅中文公开名

def 附件标识(值):#校验并构造存储标识
    """把后端产出的不透明标识做成附件标识。"""
    return 标识构造(值)#标识构造

def 图像变体标识(值):#校验并构造请求图像变换标识
    """把附件提供方产出的不透明变换标识做成图像变体标识。"""
    return 标识构造(值)#标识构造
