"""DeepSeek Harness 的出站 HTTP 代理支持。

Node 内置的 fetch 会忽略 HTTP_PROXY 等变量，因此无论用户导出了什么，Harness 请求都会直连。启动器从启动环境解析出一份策略，并装成 undici 的全局 dispatcher——这正是 fetch 解析的对象——于是 LLM 适配器、web 搜索、走 HTTP 的 MCP 与遥测都无需改动即可覆盖。

Python 侧无 undici 全局 dispatcher：安装半边以进程级策略记录与环境发布表达等价语义，路由经按网址取代理路由查询。

本包是库而非插件：传输策略每个进程只有一个答案，组合层没有可挂载、可替换或可收窄作用域的对象。

四个函数对应四种用法：安装策略、询问单次请求怎么发、构造子进程环境、以及为重放清掉环境中的代理名。
"""
from .安装 import (#对外再导出安装面
    清空代理环境,#为重放清掉全部代理环境名
    从环境安装代理,#从启动环境解析并安装
    子进程代理环境,#派生子进程所需的代理环境
    按网址取代理路由,#询问单次请求怎么发
    代理路由,#单次请求的路由答案（名义类型）
)#从安装模块导出
__all__=[#仅中文公开名
    '清空代理环境','从环境安装代理','子进程代理环境','按网址取代理路由','代理路由',
]#公开面结束
