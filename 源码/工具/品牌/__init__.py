"""`带品牌` 名义类型原语——仅类型工具（无运行时代码、无 harness 包依赖），供每个拥有跨边界 id 的包共用。

品牌让结构相同的字符串在类型层不可互换：`SessionId` 不能传入期望 `CallId` 的位置，尽管运行时两者都是普通字符串。构造走拥有包内的按 id 工厂（内部一次普通断言——零运行时成本）；比较、日志与序列化均按普通字符串处理。

政策：包为自己拥有的 id 打品牌——dsh-llm 的 `CallId`（工具调用关联）、dsh-session 的共享智能体/会话 `SessionId`、dsh-jobs 的 `JobId`。品牌用于跨包边界且可能被混淆的 id；并非每个字符串都需要品牌。
本包只拥有原语——没有具体 id，除（会被擦除的）类型外没有运行时代码——因此品牌词汇保持无依赖，包可以给自己的 id 打品牌而不依赖无关能力包。
"""
__all__=['带品牌']#仅中文公开名

# 上游 TypeScript 用 declare const BRAND: unique symbol 做编译期唯一品牌符号，运行时擦除；Python 无等价符号，仅保留名义约定。

def 带品牌(原始):#把字符串打成携带仅编译期品牌的名义类型，不做校验
    """携带仅编译期品牌的字符串。原始不透明 id 原样返回；零运行时成本（对齐上游 `Branded<B>`）。"""
    return 原始#原样返回，编译期品牌在 Python 中无运行时成本
