"""溢出策略插件的词汇：策略为溢出产物派生所属会话所需的工具执行最小结构视图。

`tools` 的 ToolExecution 满足此形态，因此策略直接读 exec，不导入 tools 或 agent。只读会话头 id——每个其他子系统都键的同一身份（见 tool-bash 的所有者派生）。
"""
溢出策略执行字段=('agent',)#策略所见的执行：存在时所属会话的头 id 经 agent.session.header.id
溢出策略智能体字段=('session',)#可选智能体：为其跑调用的智能体，若有
溢出策略会话字段=('header',)#会话
溢出策略会话头字段=('id',)#规范会话身份——溢出所有者
