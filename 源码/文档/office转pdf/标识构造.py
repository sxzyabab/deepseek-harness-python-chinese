"""源定位与转换器拥有的身份，供共享 PDF 复用。"""
from ...工具.标识构造 import 标识构造#名义字符串构造

__all__=['office源键','office转pdf世代','office转pdf键']#仅中文公开名

def office源键(键):
    """标注已授权源定位，供读前去重；授权仍由调用方负责。"""
    return 标识构造(键)#名义源键

def office转pdf世代(值):
    """标注提供方寿命（引擎、渲染与字体配置）。"""
    return 标识构造(值)#名义世代

def office转pdf键(值):
    """标注转换器拥有的内容身份；调用方不得解析。"""
    return 标识构造(值)#名义转换键
